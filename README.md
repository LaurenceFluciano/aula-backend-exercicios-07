# Exercicios

1) Interpretando um contrato de API — a partir de um contrato dado (PUT /pedidos/{id}/cancelar), identificar método, rota, corpo esperado e status de sucesso.

R: PUT = método; Rota = /pedidos/{id}/cancelar; Corpo esperado = application/json; status = 202; 

2) Convertendo um objeto Java em JSON — usar o Jackson (ObjectMapper) para serializar e desserializar a classe Produto. Projeto completo em exercicio-json.

R: Efetuado no código.

Desafio final: montando seu primeiro contrato de API — descrever o contrato completo (rota, método, corpo e status) de uma API de lista de tarefas (criar, listar, concluir). Este contrato será implementado de verdade nas próximas aulas, com Spring Web.
